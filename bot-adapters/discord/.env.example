# Discord bot token — from the Discord Developer Portal (Bot tab).
# Never commit a real token.
DISCORD_TOKEN=

# Client ID, only needed by deploy-commands.ts to register slash commands.
DISCORD_CLIENT_ID=

# Optional: register commands to one guild instantly during dev, instead of
# waiting for global command propagation (deploy-commands.ts only).
DISCORD_GUILD_ID=
